<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF3E0">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- HEADER SUKSES -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#a84622"
            android:padding="20dp"
            android:gravity="center"
            android:layout_marginBottom="16dp">



            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pesanan Berhasil!"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@android:color/white" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Terima kasih telah memesan Risol Bu Tatu"
                android:textSize="13sp"
                android:textColor="#FFE0CC" />

        </LinearLayout>

        <!-- CONTAINER FRAGMENT DETAIL PESANAN -->
        <FrameLayout
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp" />

        <!-- TOMBOL SHARE -->
        <Button
            android:id="@+id/btnShare"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Bagikan Pesanan"
            android:textColor="@android:color/white"
            android:backgroundTint="#a84622"
            android:layout_marginBottom="8dp" />

        <!-- TOMBOL KIRIM EMAIL -->
        <Button
            android:id="@+id/btnEmail"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Konfirmasi via Email"
            android:textColor="@android:color/white"
            android:backgroundTint="#1976D2"
            android:layout_marginBottom="8dp" />

        <!-- TOMBOL KEMBALI -->
        <Button
            android:id="@+id/btnKembali"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="⬅Pesan Lagi"
            android:textColor="#a84622"
            android:backgroundTint="@android:color/white"
            android:layout_marginBottom="24dp" />

    </LinearLayout>
</ScrollView>
